[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --strict-markers
markers =
    slow: tests that are slow to run
    webrtc: tests requiring webcam
    integration: integration tests